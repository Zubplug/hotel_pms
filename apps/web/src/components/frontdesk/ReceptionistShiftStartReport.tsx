'use client';

import { useEffect, useMemo, useState } from 'react';
import { Button } from '@/components/ui/button';
import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { useLodgeCoreProvider } from '@/lib/desktop/DataProviderContext';
import { formatRoomNumber } from '@/lib/format-room';
import { Printer, RefreshCw } from 'lucide-react';

export function ReceptionistShiftStartReport({ open, onOpenChange, dashboardData, propertyId }: { open: boolean; onOpenChange: (open: boolean) => void; dashboardData: any; propertyId: string }) {
  const { provider } = useLodgeCoreProvider();
  const [rooms, setRooms] = useState<any[]>([]);
  const [loadingRooms, setLoadingRooms] = useState(false);
  const loadRooms = async () => { setLoadingRooms(true); try { const result = await provider.rooms.list(propertyId); setRooms(Array.isArray(result) ? result : result?.data || []); } finally { setLoadingRooms(false); } };
  useEffect(() => { if (open) void loadRooms(); }, [open, propertyId]);
  const availableRooms = useMemo(() => rooms.filter(room => ['AVAILABLE', 'CLEAN'].includes(String(room.status).toUpperCase())), [rooms]);
  const arrivals = dashboardData?.arrivals || [];
  const departures = dashboardData?.departures || [];
  const money = (value: unknown) => `${dashboardData?.property?.currency || 'NGN'} ${Math.abs(Number(value || 0)).toLocaleString(undefined, { minimumFractionDigits: 2 })}`;
  const balanceLabel = (value: unknown) => { const balance = Number(value || 0); if (balance > 0) return { text: `${money(balance)} debt`, className: 'text-rose-700' }; if (balance < 0) return { text: `${money(balance)} credit`, className: 'text-emerald-700' }; return { text: 'Settled', className: 'text-emerald-700' }; };

  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-5xl max-h-[92vh] overflow-y-auto p-0 print-report-shell"><div className="flex items-center justify-between border-b bg-slate-950 px-6 py-4 text-white print:hidden"><DialogHeader><DialogTitle className="text-white">Shift Start Report</DialogTitle></DialogHeader><div className="flex gap-2"><Button variant="outline" size="sm" onClick={() => void loadRooms()} disabled={loadingRooms} className="border-white/30 bg-white/10 text-white hover:bg-white/20"><RefreshCw className="mr-2 h-4 w-4" />Refresh</Button><Button size="sm" onClick={() => window.print()} className="bg-blue-600 text-white hover:bg-blue-500"><Printer className="mr-2 h-4 w-4" />Print A4 report</Button></div></div><div className="shift-report-print bg-white p-8 text-slate-900 sm:p-10"><div className="flex items-start justify-between border-b-2 border-slate-900 pb-5"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-blue-700">LodgeCore PMS</p><h1 className="mt-1 text-3xl font-black">Reception Shift Start Report</h1><p className="mt-1 text-sm text-slate-500">Daily operational brief · {new Date(dashboardData?.businessDate || Date.now()).toLocaleDateString('en-GB', { weekday: 'long', day: '2-digit', month: 'long', year: 'numeric' })}</p></div><div className="text-right text-sm"><p className="font-bold">{dashboardData?.property?.name || 'Property'}</p><p className="text-slate-500">Prepared {new Date().toLocaleTimeString('en-GB', { hour: '2-digit', minute: '2-digit' })}</p></div></div><div className="mt-6 grid grid-cols-4 gap-3">{[['Arrivals', arrivals.length], ['Departures', departures.length], ['Available rooms', availableRooms.length], ['Total rooms', rooms.length]].map(([label, value]) => <div key={String(label)} className="rounded-lg border border-slate-200 p-3"><p className="text-[10px] font-bold uppercase tracking-wider text-slate-500">{label}</p><p className="mt-1 text-2xl font-black">{value}</p></div>)}</div><section className="mt-7"><h2 className="border-b border-slate-300 pb-2 text-lg font-bold">Today’s arrivals</h2><ReportTable rows={arrivals} kind="arrival" balanceLabel={balanceLabel} /></section><section className="mt-7"><h2 className="border-b border-slate-300 pb-2 text-lg font-bold">Today’s departures</h2><ReportTable rows={departures} kind="departure" balanceLabel={balanceLabel} /></section><section className="mt-7"><h2 className="border-b border-slate-300 pb-2 text-lg font-bold">Available rooms at shift start</h2>{loadingRooms ? <p className="py-4 text-sm text-slate-500">Loading room inventory…</p> : <div className="mt-3 grid grid-cols-4 gap-2">{availableRooms.map(room => <div key={room.id} className="rounded border border-slate-200 px-3 py-2 text-sm"><span className="font-bold">{formatRoomNumber(room.number)}</span><span className="ml-2 text-slate-500">{room.roomType?.name || room.roomTypeName || ''}</span></div>)}</div>}</section><div className="mt-10 border-t border-slate-300 pt-3 text-xs text-slate-500">Operational use only · Verify balances and room readiness before check-in. Generated by LodgeCore PMS.</div></div><style jsx global>{`@media print { @page { size: A4; margin: 12mm; } body * { visibility: hidden !important; } .shift-report-print, .shift-report-print * { visibility: visible !important; } .shift-report-print { position: absolute !important; inset: 0 !important; width: 100% !important; padding: 0 !important; } .print-report-shell { position: static !important; max-width: none !important; max-height: none !important; overflow: visible !important; border: 0 !important; box-shadow: none !important; } }`}</style></DialogContent></Dialog>;
}

function ReportTable({ rows, kind, balanceLabel }: { rows: any[]; kind: 'arrival' | 'departure'; balanceLabel: (value: unknown) => { text: string; className: string } }) {
  if (!rows.length) return <p className="py-4 text-sm text-slate-500">No {kind}s scheduled for today.</p>;
  return <table className="mt-3 w-full text-left text-xs"><thead className="border-b border-slate-200 text-[10px] uppercase tracking-wider text-slate-500"><tr><th className="py-2">Guest</th><th>Phone</th><th>Room</th><th>{kind === 'arrival' ? 'Arrival' : 'Departure'}</th><th>Room status</th><th className="text-right">Folio balance</th></tr></thead><tbody className="divide-y divide-slate-100">{rows.map(row => { const balance = balanceLabel(row.balance); return <tr key={row.id}><td className="py-2 font-semibold">{row.guestName}</td><td className="font-mono">{row.guestPhone || '—'}</td><td>{formatRoomNumber(row.roomName) || 'Unassigned'}</td><td>{kind === 'arrival' ? row.arrivalTime || '—' : row.checkOutTime || '—'}</td><td>{String(row.roomStatus || 'UNKNOWN').replaceAll('_', ' ')}</td><td className={`text-right font-semibold ${balance.className}`}>{balance.text}</td></tr>; })}</tbody></table>;
}
