<iframe src="@WebAppUrl" />

@code {
    // For now, load the local Next.js instance. 
    // In production, this would be read from appsettings.json or point to the hosted app.
    private string WebAppUrl = "http://localhost:3000";
}
